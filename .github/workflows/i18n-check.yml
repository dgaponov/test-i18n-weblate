name: i18n-check
on:
    pull_request:
        branches:
            main

jobs:
  i18n-check-run:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Hello world action step
        id: hello
        uses: dgaponov/weblate-action@v1.1
        with:
            serverUrl: "http://127.0.0.1"
            token: "sometoken123"
            project: "datalens"
            PR_NO: ${{ github.event.pull_request.number }}
